---
:service_menu: open_konsole_here
:entry:
  :submenu: Open Konsole Here
:common:
  select_session: Select the Konsole session
:actions:
  last_inst_current_sess:
    :name: Current session (last instance)
    title:
  last_inst_new_sess:
    :name: New session (last instance)
    title:
  first_inst_current_sess:
    :name: Current session (first instance)
    title:
  first_inst_new_sess:
    :name: New session (first instance)
    title:
  select_inst_current_sess:
    :name: Current session (select the instance)
    title: Open Konsole Here - Current session
  select_inst_new_sess:
    :name: New session (select the instance)
    title: Open Konsole Here - New session